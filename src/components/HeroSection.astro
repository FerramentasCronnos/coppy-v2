<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">
        <span class="brand-name">COPY CA$H</span>
        <span class="main-headline">ESSE SISTEMA DE RECONHECIMENTO DE PADRÕES ESTÁ 
PERMITINDO QUE PESSOAS COMUNS FINALMENTE TENHAM 
RESULTADOS CONSISTENTES NO DAY TRADE</span>
      </h1>
      
      <p class="hero-subtitle">Descubra como a identificação correta de padrões do mercado permite que pessoas sem experiência faturem entre R$300 e R$1.247 por dia nos mercado financeiro, mesmo sem conhecimento técnico.</p>
      
      <div class="hero-highlight">
        <p><b>Você já se perguntou por que algumas pessoas conseguem fazer R$1.000 em 1 hora enquanto você batalha 8 horas por dia para ganhar o mesmo?</b></p>
        <p><b>Não é sorte. Não é talento. Não é "contatos privilegiados".</b></p>
        <p><b>A verdade que poucos conhecem é que o dinheiro na era digital se move seguindo padrões específicos e previsíveis. Quem sabe identificar esses padrões captura oportunidades que 99% das pessoas jamais verão, mesmo que estejam bem na frente delas.</b></p>
        <p><b><i>"Achei que era tarde demais pra mim. Com 52 anos, quase me aposentando, sem entender muita coisa da internet. Mas esse sistema mudou tudo. Em 27 dias, fiz R$784 seguindo apenas os sinais no meu celular. Minha esposa sempre criticou minhas "loucuras financeiras", agora me pergunta todo dia: "E aí, quanto fizemos hoje?" - Roberto C., Curitiba, ex-funcionário público</i></b></p>
        <p><b>O reconhecimento de padrões no mercado financeiro se tornou a ferramenta secreta de quem realmente ganha dinheiro nos dias de hoje:</b></p>        
        <div class="key-point">
          <div class="checkbox-wrapper">
            <input type="checkbox" checked disabled>
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <rect class="background" width="36" height="36" x="2" y="2" rx="5"></rect>
              <rect class="stroke" width="36" height="36" x="2" y="2" rx="5"></rect>
              <polyline class="check" points="11,20 17,26 28,15"></polyline>
            </svg>
          </div>
          <p>Eles <span class="highlight">DETECTAM</span> oportunidades invisíveis. Enquanto você está preso no trânsito indo trabalhar, essa tecnologia identifica sinais digitais que aparecem todos os dias nas telas dos seus celulares.</p>
        </div>
        
        <div class="key-point">
          <div class="checkbox-wrapper">
            <input type="checkbox" checked disabled>
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <rect class="background" width="36" height="36" x="2" y="2" rx="5"></rect>
              <rect class="stroke" width="36" height="36" x="2" y="2" rx="5"></rect>
              <polyline class="check" points="11,20 17,26 28,15"></polyline>
            </svg>
          </div>
          <p>Funcionam em <span class="highlight">QUALQUER</span> cenário econômico. Crise? Pandemia? Inflação? O sistema continua funcionando porque se adapta automaticamente às mudanças do mercado digital.</p>
        </div>
        
        <div class="key-point">
          <div class="checkbox-wrapper">
            <input type="checkbox" checked disabled>
            <svg width="40" height="40" viewBox="0 0 40 40" aria-hidden="true">
              <rect class="background" width="36" height="36" x="2" y="2" rx="5"></rect>
              <rect class="stroke" width="36" height="36" x="2" y="2" rx="5"></rect>
              <polyline class="check" points="11,20 17,26 28,15"></polyline>
            </svg>
          </div>
          <p>Revelam o <span class="highlight">MOMENTO EXATO DE AGIR.</span> Você já perdeu dinheiro agindo por impulso ou emoção? Este sistema mostra exatamente quando entrar e quando sair, eliminando o fator emocional.</p>
        </div>
      </div>
      
      <div class="benefits-list">
        <div class="notification-alert">
          <div class="notification-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"></path>
            </svg>
          </div>
          <div class="notification-content">
            <p class="notification-title">Sinal 1: DETECTA ANOMALIAS DE FLUXO</p>
            <p class="notification-desc">O sistema identifica quando grandes instituições deixam "rastros" digitais que sinalizam movimentações massivas antes que aconteçam. É como ver o tsunami enquanto ainda é apenas uma ondulação no oceano.</p>
          </div>
        </div>
        
        <div class="notification-alert">
          <div class="notification-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"></path>
            </svg>
          </div>
          <div class="notification-content">
            <p class="notification-title">Sinal 2: CONFIRMAÇÃO MULTI-FONTE</p>
            <p class="notification-desc">Nosso algoritmo cruza 5 fontes de dados independentes para confirmar que o movimento identificado é realmente válido e não um "falso positivo".</p>
          </div>
        </div>
        
        <div class="notification-alert">
          <div class="notification-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5"></path>
            </svg>
          </div>
          <div class="notification-content">
            <p class="notification-title">Sinal 3: ACIONAMENTO PRECISO</p>
            <p class="notification-desc">Você recebe uma notificação exata com instruções passo-a-passo sobre o que fazer, quando fazer, quanto investir e quando retirar seus ganhos.</p>
          </div>
        </div>
      </div>
      
      <div class="cta-container">
        <a href="https://clkdmg.site/checkouts/ab-order-bump-x" class="btn-cta">
          <span class="shadow"></span>
          <span class="edge"></span>
          <span class="front">QUERO RECEBER OS SINAIS DO PADRÃO LUCRATIVO</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Variáveis CSS para melhor manutenção e redução de duplicação */
  :root {
    --color-primary: #00c31d;
    --color-primary-dark: #00a318;
    --color-accent: #FF6B00;
    --color-accent-dark: #E05A00;
    --color-bg-dark: rgba(0, 0, 0, 0.4);
    --color-bg-darker: rgba(0, 0, 0, 0.3);
    --color-text: #ffffff;
    --color-text-light: rgba(255, 255, 255, 0.8);
    --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.2);
    --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.3);
    --shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.4);
    --radius-sm: 8px;
    --radius-md: 10px;
    --radius-lg: 12px;
    --transition-fast: all 0.2s ease;
    --transition-normal: all 0.3s ease;
    --transition-slow: all 0.6s cubic-bezier(.3, .7, .4, 1);
  }

  .hero {
    padding: 2rem 0;
    overflow: hidden;
  }
  
  .hero-content {
    max-width: 850px;
    margin: 0 auto;
    text-align: center;
  }
  
  .brand-name {
    display: block;
    font-size: clamp(2.2rem, 5vw, 3.2rem);
    font-weight: 800;
    margin-bottom: 1rem;
    background: linear-gradient(90deg, #FFD700, #FFA500);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 2px 10px rgba(255, 215, 0, 0.3);
    letter-spacing: 1px;
  }
  
  .hero-title {
    font-size: clamp(1.5rem, 4vw, 2rem);
    font-weight: 800;
    line-height: 1.3;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
  }
  
  .main-headline {
    display: block;
    margin-top: 1rem;
    letter-spacing: 0.5px;
  }
  
  .hero-subtitle {
    font-size: clamp(1.2rem, 3vw, 1.4rem);
    font-weight: 500;
    margin-bottom: 1.5rem;
    color: var(--color-text-light);
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .hero-highlight {
    font-size: clamp(1.1rem, 2.5vw, 1.2rem);
    color: var(--color-text);
    margin-bottom: 1.5rem;
    background-color: var(--color-bg-dark);
    padding: 1.5rem;
    border-radius: var(--radius-md);
    border-left: 4px solid var(--color-accent);
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    text-align: left;
  }
  
  .hero-highlight p {
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  
  .hero-highlight p:last-child {
    margin-bottom: 0;
  }
  
  .key-point {
    display: flex;
    align-items: flex-start;
    margin-bottom: 1.2rem;
    position: relative;
    background: var(--color-bg-darker);
    border-radius: var(--radius-sm);
    padding: 10px;
    border: 1px solid rgba(0, 150, 0, 0.2);
    box-shadow: var(--shadow-sm);
    transition: var(--transition-normal);
  }
  
  .key-point:hover {
    transform: translateX(5px);
    background: var(--color-bg-dark);
    border-color: rgba(0, 200, 0, 0.3);
  }
  
  .key-point p {
    margin: 0;
    padding: 8px 0;
    font-weight: 500;
  }
  
  .highlight {
    color: var(--color-primary);
    font-weight: 700;
    padding: 0 3px;
    position: relative;
    text-shadow: 0 0 8px rgba(0, 255, 0, 0.4);
  }
  
  .explanation {
    font-weight: normal;
    font-style: italic;
    font-size: 0.9em;
    color: var(--color-text-light);
  }
  
  /* Checkbox styles - otimizados */
  .checkbox-wrapper {
    position: relative;
    display: inline-block;
    width: 40px;
    height: 40px;
    margin-right: 12px;
    flex-shrink: 0;
  }

  .checkbox-wrapper .background {
    fill: var(--color-primary);
    transition: var(--transition-normal);
  }

  .checkbox-wrapper .stroke {
    fill: none;
    stroke: var(--color-primary);
    stroke-miterlimit: 10;
    stroke-width: 2px;
    stroke-dasharray: 100;
    transition: var(--transition-normal);
  }

  .checkbox-wrapper .check {
    fill: none;
    stroke: white;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2px;
    stroke-dasharray: 22;
    transition: var(--transition-normal);
  }

  .checkbox-wrapper input[type="checkbox"] {
    position: absolute;
    width: 100%;
    height: 100%;
    left: 0;
    top: 0;
    margin: 0;
    opacity: 0;
    -appearance: none;
    -webkit-appearance: none;
  }
  
  /* Notification style - otimizado */
  .benefits-list {
    max-width: 700px;
    margin: 2rem auto;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  
  .notification-alert {
    display: flex;
    align-items: flex-start;
    width: 100%;
    min-height: 70px;
    border-radius: var(--radius-sm);
    background-color: #232531;
    padding: 16px;
    box-shadow: var(--shadow-md);
    transition: var(--transition-normal);
    text-align: left;
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(43, 152, 117, 0.1);
  }
  
  .notification-alert::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(to bottom, #2b9875, #1a5e49);
  }
  
  .notification-alert:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
    background-color: #2a2c3b;
    border-color: rgba(43, 152, 117, 0.3);
  }
  
  .notification-icon {
    flex-shrink: 0;
    color: #2b9875;
    background-color: rgba(43, 152, 117, 0.1);
    backdrop-filter: blur(10px);
    padding: 8px;
    border-radius: var(--radius-sm);
    margin-right: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
  }
  
  .notification-content {
    flex: 1;
  }
  
  .notification-title {
    color: white;
    font-weight: 700;
    font-size: 1.1rem;
    margin-bottom: 8px;
    line-height: 1.3;
  }
  
  .notification-desc {
    color: #8a8c99;
    font-size: 0.95rem;
    line-height: 1.5;
  }
  
  .cta-container {
    margin-top: 2rem;
    transform: scale(1.1);
  }
  
  /* Estilos otimizados para o botão CTA */  
  .btn-cta {
    position: relative;
    border: none;
    background: transparent;
    padding: 0;
    cursor: pointer;
    outline-offset: 4px;
    transition: filter 250ms;
    user-select: none;
    touch-action: manipulation;
    display: inline-block;
    font-weight: 700;
    text-decoration: none;
  }
  
  .shadow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: var(--radius-lg);
    background: hsl(0deg 0% 0% / 0.25);
    will-change: transform;
    transform: translateY(2px);
    transition: var(--transition-slow);
  }
  
  .edge {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: var(--radius-lg);
    background: linear-gradient(
      to left,
      hsl(25deg 100% 16%) 0%,
      hsl(25deg 100% 32%) 8%,
      hsl(25deg 100% 32%) 92%,
      hsl(25deg 100% 16%) 100%
    );
  }
  
  .front {
    display: block;
    position: relative;
    padding: 16px 36px;
    border-radius: var(--radius-lg);
    font-size: clamp(1.1rem, 2.5vw, 1.3rem);
    color: white;
    background: var(--color-accent);
    will-change: transform;
    transform: translateY(-4px);
    transition: var(--transition-slow);
    text-transform: uppercase;
  }
  
  .btn-cta:hover {
    filter: brightness(110%);
  }
  
  .btn-cta:hover .front {
    transform: translateY(-6px);
    transition: transform 250ms cubic-bezier(.3, .7, .4, 1.5);
  }
  
  .btn-cta:active .front {
    transform: translateY(-2px);
    transition: transform 34ms;
  }
  
  .btn-cta:hover .shadow {
    transform: translateY(4px);
    transition: transform 250ms cubic-bezier(.3, .7, .4, 1.5);
  }
  
  .btn-cta:active .shadow {
    transform: translateY(1px);
    transition: transform 34ms;
  }
  
  .btn-cta:focus:not(:focus-visible) {
    outline: none;
  }
  
  /* Media queries otimizadas */
  @media (max-width: 767px) {
    .hero-highlight {
      padding: 1rem;
    }
    
    .notification-alert {
      min-height: 60px;
      padding: 10px;
    }
    
    .notification-icon {
      padding: 6px;
      margin-right: 10px;
    }
    
    .notification-title {
      font-size: 0.95rem;
    }
    
    .notification-desc {
      font-size: 0.85rem;
    }
    
    .front {
      padding: 14px 28px;
    }
    
    .cta-container {
      transform: scale(1);
    }
    
    .checkbox-wrapper {
      width: 30px;
      height: 30px;
    }
    
    .checkbox-wrapper svg {
      width: 30px;
      height: 30px;
    }
  }
</style>
